
/**
 * Per-session database of the voluminous crash data we have retrieved from
 * crash-stats.mozilla.com and derived computations that can be re-derived from
 * data stored in the RootDB that stores user-created data.
 */
class CrashDB {
  constructor(sessionName) {

  }
}
